package org.lyranthe.prometheus.client.counter

import org.lyranthe.prometheus.client._

case class UnlabelledCounter private[client] (name: MetricName, help: String) {
  def labels(): Counter0 = Counter0(name, help)

  [#
  def labels([#labelName1: LabelName#]): Counter1 = Counter1(name, help, List([#labelName1#]))
  #
  ]
}
