package org.lyranthe.prometheus.client.internal.gauge

import org.lyranthe.prometheus.client.internal.{LabelName, MetricName}

private[client] case class UnlabelledGauge(name: MetricName, help: String) {
  def labels(): Gauge0 = Gauge0(name, help)

  [#
  def labels([#labelName1: LabelName#]): Gauge1 = Gauge1(name, help, List([#labelName1#]))
  #
  ]
}
