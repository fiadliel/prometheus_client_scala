package io.prometheus.client.scala

import io.prometheus.client.scala.internal.counter._

object Counter {
  def apply(name: String): Counter0 = new Counter0(name)

  [#
  def apply(name: String, [#labelName1: String#]): Counter1 = new Counter1(name)(List([#labelName1#]))
  #
  ]
}
